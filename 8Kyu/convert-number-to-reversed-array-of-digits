def digitize(n):
    out =[]
    nString = str(n) #convert input number to string for slicing
    for d in range(-1,-len(nString)-1,-1): # create a list of integers from -1 to the length of string to use as the index of the n
        out.append(int(nString[d]))
    return out
'''
Convert number to reversed array of digits
Given a random non-negative number, you have to return the digits of this number within an array in reverse order.

Example(Input => Output):
35231 => [1,3,2,5,3]
0 => [0]
'''